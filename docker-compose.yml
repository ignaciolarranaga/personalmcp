services:
  personalmcp:
    build: .
    image: personalmcp
    ports:
      - "3000:3000"
    volumes:
      - ./models:/app/models
      - ./memory:/app/memory
      - ./config.yaml:/app/config.yaml:ro
